import React from 'react';
import type { MainContentCard } from '../../../types';
import StudioSettings from '../../../components/custom/StudioSettings';

export const studioCards: MainContentCard[] = [
    { 
        id: `studio-settings-view`,
        component: StudioSettings
    },
];
